# Shell-Variablen für die Bash-Skripte (create.sh / drop.sh / deploy.sh).
# Läuft auf dem Hetzner-Host; das Postgres ist dort lokal -> DB_HOST=localhost.
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_NAME=di2f_prod

APP_VERSION_MAJOR=1
APP_VERSION_MINOR=0
APP_VERSION_BUILD=0
